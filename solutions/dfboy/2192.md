---
title: "哈夫曼树（2）"
tags: ["二叉树"]
difficulty: 入门
source: dfboy
problem_id: 2192
---

# 2192 - 哈夫曼树（2）

## 题目描述

哈夫曼树的定义是：一棵具有n个带权叶结点的二叉树，是的所有叶结点的带权路径长度（叶结点×叶结点到根结点的路径长度）之和最小，这样的二叉树被称为最优二叉树，也称哈夫曼树。

比如：有4个结点的权值是5 4 2 9，可以构建出如下三颗不同的二叉树，第2棵二叉树的带权路径长度是最小的。

请读入一个整数n，代表叶结点的数量，再读入n个整数，代表叶结点的权值，请求出对应哈夫曼树的带权路径长度。

## 输入

第1行有一个整数m（m≤20），表示有m组测试数据。

每组第一行输入一个数n，接着输入n个叶节点的权值（叶节点权值不超过100，2<=n<=1000）。

## 输出

输出每组数据对应哈夫曼树的带权路径长度，每行一个。
## AC代码

```cpp
#include <iostream>
#include <queue>   // priority_queue 需要
#include <vector>  // priority_queue 的底层容器
using namespace std;

int main(){
    int m;          // 测试数据组数
    cin >> m;
    
    while (m--){
        int n;      // 叶结点个数
        cin >> n;
        
        // 小顶堆：priority_queue<int, vector<int>, greater<int> >
        // 默认是大顶堆（最大元素在堆顶），加 greater<int> 就变成小顶堆
        priority_queue<int, vector<int>, greater<int> > pq;
        
        // 读入 n 个叶结点的权值，放入小顶堆
        for (int i = 1; i <= n; i++){
            int x;  // 临时存每个权值
            cin >> x;
            pq.push(x);
        }
        
        int ans = 0;  // 存最终答案（带权路径长度 WPL）
        
        // 当堆中还有多于 1 个元素时，继续合并
        // 只剩 1 个元素说明哈夫曼树已构建完成
        while (pq.size() > 1){
            // 取出当前最小的两个结点
            int a = pq.top(); pq.pop();
            int b = pq.top(); pq.pop();
            
            int sum = a + b;   // 合并后的新结点权值
            ans += sum;        // ★ 关键：累加合并权值到答案中
            pq.push(sum);      // 新结点放回堆中
        }
        
        cout << ans << endl;   // 输出每组的结果
    }
    
    return 0;
}
```

