---
title: "成绩排序"
tags: ["蓝桥杯等级考试", "结构体", "数组问题"]
difficulty: 入门
source: dfboy
training: ["蓝桥杯STEAM"]
problem_id: 2356
---

# 2356 - 成绩排序

## 题目描述

【具体要求】  

虽然现在学校已经不允许将学生每次的考试成绩公布，但是尽职尽责的班主任小青老师还是会私下将学生们的成绩排序，看看哪些学生进步了，哪些学生有所退步，针对退步的学生想一些提升方案。  

现在请你通过编程来帮助小青老师实现全班 $n$ 位同学成绩的排名。排名规则为总分从高到低排序，当出现总成绩相同时，根据语文成绩从高到低排序；当总成绩相同且语文成绩也相同时按照输入的先后顺序排序。

## 输入

输入共 $n+1$ 行,第一行输入一个正整数 $n$ （$1 \lt n \lt 100$）表示全班学生数量；  

第 $2 \sim n+1$ 行，每行分别是学生姓名 $m$（$1 \lt m \lt 20$）、语文成绩、数学成绩、英语成绩，中间用空格隔开，各科成绩满分 $100$ 分。

## 输出

输出共 $n$ 行，根据题目要求的排序规则输出，每行分别为学生姓名、总成绩、语文成绩。
## AC代码
## AC代码

```cpp
#include <iostream>
#include <iomanip>
#include <bits/stdc++.h>
using namespace std;

struct Student {
    string name; // 姓名
    int num; // 输入序号
    int chinese; // 语文
    int math; // 数学
    int english; // 英语
    int sum; // 总分
}a[200];

bool cmp(Student x, Student y) {
    if(x.sum < y.sum || (x.sum == y.sum && x.chinese < y.chinese) || (x.sum == y.sum && x.chinese == y.chinese && x.num > y.num)) {
        return false;
    } else {
        return true;
    }  
}

int main() {
    int i,n;
    cin >> n;
    for(i = 1; i <= n; i++) {
        cin >> a[i].name >> a[i].chinese >> a[i].math >> a[i].english;
        a[i].sum = a[i].chinese + a[i].math + a[i].english;
        a[i].num = i;
    }
    // 排序
    sort(a + 1, a + n + 1, cmp);
    for(i = 1; i <= n;i++) {
        cout << a[i].name << " " << a[i].sum << " " << a[i].chinese << endl;
    }
}
```

