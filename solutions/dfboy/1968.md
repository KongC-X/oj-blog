---
title: "输出ascii码对应的字符"
tags: ["字符型"]
difficulty: 入门
source: dfboy
training: ["字符串"]
problem_id: 1968
---

# 1968 - 输出ascii码对应的字符

## 题目描述

从键盘读入一个整数（ascii 码），请计算并输出该 ascii 码对应的字符。

比如：ascii 码 $65$ 对应的字符是 '$A$' ，$97$ 对应的字符是 '$a$' ，$48$ 对应的字符是 '$0$' 。

## 输入

一个整数（值在ascii码表的范围内）。

## 输出

该ascii码对应的字符。
## AC代码
## AC代码

```cpp
#include<iostream>
using namespace std;

int main(){
    int x;
    char a;
    cin >> x;
    a = x;
    cout << a << endl;
}
```

