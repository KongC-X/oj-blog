---
title: "最长不下降子序列（LIS）"
tags: ["动态规划"]
difficulty: 入门
source: dfboy
problem_id: 1794
---

# 1794 - 最长不下降子序列（LIS）

## 题目描述

设有由 $n$ 个不相同的整数组成的数列，记为: $a_1$、$a_2$、$ \dots $、$a_n$ 且 $a_i \neq a_j$ ($i \neq j$)。

例如 $3$，$18$，$7$，$14$，$10$，$12$，$23$，$41$，$16$，$24$。

若存在 $i_1 \lt i_2 \lt i_3 \lt \dots \lt i_e$ 且有$a$$i1$$\lt a$$i2$$\lt \dots \lt a$$ie$，则称为长度为 $e$ 的不下降序列。

如上例中 $3$，$18$，$23$，$24$ 就是一个长度为 $4$ 的不下降序列，同时也有 $3$，$7$，$10$，$12$，$16$，$24$ 长度为 $6$ 的不下降序列。

程序要求，当原数列给出之后，求出最长的不下降序列。

## 输入

第一行为 $n$，表示 $n$ 个数（$10 \le n \le 10000$）；

第二行 $n$ 个整数，数值之间用一个空格分隔（$1 \le a_i  \le n$）；

## 输出

最长不下降子序列的长度。

## 解题思路

## AC代码
## AC代码

```cpp
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int a[10010],n,i,j;
int dp[10010];
int ma,s;

int main() {
    cin >> n;
    for(i = 1;i <= n;i++) {
        cin >> a[i];
    }

    // 状态转移方程：
    // dp[i] = max(dp[j] + 1, dp[i])
    // 其中 0 <= j < i 且 a[j] < a[i]
    // 求以每个数结尾的最长上升子序列的长度
    for(i = 1;i <= n;i++) {
        dp[i] = 1;
        // 将a[i]尝试续到每个数后面，看dp[i]能否增加
        for(j = 1;j < i;j++) {
            if(a[j] < a[i]) {
                dp[i] = max(dp[j] + 1, dp[i]);
            }
        }
        ma = max(ma, dp[i]);
    }

    cout << ma << endl;
}
```

