---
title: "删除指定字符"
tags: ["字符串"]
difficulty: 入门
source: dfboy
problem_id: 1131
---

# 1131 - 删除指定字符

## 题目描述

从键盘输入一个字符串 $str$ 和一个字符 $c$ ，删除 $str$ 中的所有字符 $c$ 并输出删除后的字符串 $str$ 。

## 输入

第一行是一个字符串，字符串不含空格，长度 $ \le 100$ ；

第二行是一个字符。

## 输出

删除指定字符后的字符串。

## 解题思路

## AC代码

```cpp
#include<iostream>
using namespace std;

int main() {
    int m,n,s = 0,sum = 0;
    cin >> n;
    while(n != s) {
        s = 0; // 每次清空s
        m = n; // 不能修改n的值
        while(m != 0) {
            s = s * 10 + m % 10;
            m /= 10;
        }
        if(n != s) {
            n = n + s; // 如果不相等那么就将n和颠倒n相加
        }
        sum++;
        // cout << n << endl;
    }
    cout << sum - 1 << endl; // 要减去最后一次，因为最后一次比较了不相等
}
```
