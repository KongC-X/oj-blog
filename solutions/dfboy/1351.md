---
title: "买公园门票"
tags: ["需要找规律的循环"]
difficulty: 入门
source: dfboy
problem_id: 1351
---

# 1351 - 买公园门票

## 题目描述

某公园门票价格为：成人票 $8$ 元 $/$ 张，儿童票 $3$ 元 $/$ 张；某旅游团来公园游玩，该团内有成人和儿童（成人和儿童都有），共花了 $40$ 元买门票。

请你分别计算出成人和儿童可能的人数，按照成人从少到多，儿童从多到少的规律数出结果。

（ $7.2$ ）

## 输入

无

## 输出

若干行，每行 $2$ 个整数用空格隔开，分别代表成人和儿童可能的人数。（成人从少到多，儿童从多到少）

## 解题思路

## AC代码

```cpp
#include<iostream>
#include<bits/stdc++.h>
using namespace std;

// 埃氏筛法
bool f[1000010]; // 标记素数，false表示素数，true表示合数
int k,n;
int main() {
	cin >> n;
	// 特判特殊的数
	f[0] = true;
	f[1] = true;
	k = n - 1; // 初始素数个数为n-1，因为1不是素数

	// 循环所有的数，逐个判断是否为素数
	for(int i = 2; i * i <= n; i++) {
		// 如果当前数是素数，则将其所有的倍数标记为合数
	    if(!f[i]) {
			// 将i的倍数标记为合数
			for(int j = i + i; j <= n;j += i) {
				// 如果没有标记过
				if(!f[j]) {
				    f[j] = true;
				    k--; // 素数个数减一
				}
			}
	    }
	}
	cout << k << endl;
}


// 线性筛
bool f[1000010]; // 标记素数，false表示素数，true表示合数
int prime[1000010]; // 存储素数
int k,n;
// 线性筛：每个合数只能被自己的最小质因子筛掉
// 比如：6 = 2 * 3，6只能被2筛掉，不能被3筛掉
int main() {
	cin >> n;
	// 特判特殊的数
	f[0] = true;
	f[1] = true;

	// 循环所有的数，逐个判断是否为素数
	for(int i = 2; i <= n; i++) {
		// 如果当前数是素数，则将其所有的倍数标记为合数
	    if(!f[i]) {
			k++;
			// 将素数存入数组中
			prime[k] = i;
		}
		// 每个合数只能被自己的最小质因子筛掉
		// j循环的是素数，prime[j]表示第j个素数
		for(int j = 1;i * prime[j] <= n;j++) {
			f[i * prime[j]] = true; // 将这个数筛掉
			// 如果i能被prime[j]整除
			// i * prime[j+1] % prime[j] == 0
			if(i % prime[j] == 0) {
				break; // 直接跳出循环
			}
		}
	}
	cout << k << endl;
}
```
