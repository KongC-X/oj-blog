---
title: "幂和数"
tags: ["GESP二级", "25年6月", "枚举", "进制转换"]
difficulty: 入门
source: dfboy
problem_id: 4978
---

# 4978 - 幂和数

## 题目描述

对于正整数 $n$，如果 $n$ 可以表为两个 $2$ 的次幂之和，即 $n=2^{x}+2^{y}$ （ $x,y$ 均为非负整数），那么称 $n$ 为幂和数。

给定正整数 $l,r$，请你求出满足 $l \le n \le r$ 的整数 $n$ 中有多少个幂和数。

## 输入

一行，两个正整数 $l,r$ ，含义如上。

## 输出

输出一行，一个整数，表示 $l,r$ 之间幂和数的数量。
## AC代码

```cpp
#include <iostream>
using namespace std;
int main(){
    int l, r, a, b, n, cnt = 0;
    cin >> l >> r;
    a = 1;
    while(a<=r){
        b = a;
        while(b<=r) {
            n = a+b;
            if(n>=l && n<=r) cnt++;
            b *= 2;
        }
        a *= 2;
    }
    cout << cnt <<endl;
    return 0;
}
```

