---
title: "人工智能"
tags: ["市赛"]
difficulty: 入门
source: dfboy
problem_id: 1528
---

# 1528 - 人工智能

## 题目描述

人胖了就容易打瞌睡，打瞌睡又会导致长胖，这对小Ｘ的减重计划很不利，于是小Ｘ决定做点动脑子的事来抵御瞌睡虫的进袭，小Ｘ决定响应国务院的号召投身到人工智能的研究开发大潮中去，具体研究什么好呢？小Ｘ再三思考后决定开发国际象棋的人工智能软件，虽然国际象棋的软件已经能够战胜人类世界冠军了，但那是基于搜索的 AI(AI 是人工智能的简称)，小Ｘ想写一个基于机器学习的国际象棋 AI，目标是战胜 IBM 的深蓝，小Ｘ的国际象棋水平也不差，曾获得过市青少年比赛的冠军，机器学习顾名思义就是让机器像人一样学习，小Ｘ首先训练机器学习棋子对棋盘的控制，具体做法是小Ｘ先在棋盘上放置若干个车和后，然后让机器判断有多少个格子没有被车和后控制到。

车和后的吃子规则如下：车：横、竖均可以走，步数不受限制，但不能斜着走。后：横、竖、斜都可以走，步数不受限制。无论是车还是后都不能越过棋子去吃子，用作训练的国际象棋棋盘可以放大缩小，并不限于 $8 \times 8$ 的棋盘，车和后所在的位置当然是被控制的，它们能走到的位置也都被控制。

## 输入

第一行包含一个正整数 $n$，表示棋盘的大小。

接下来 $n$ 行，每行 $n$ 个整数，其中 $0$ 表示棋盘上这个格子为空的，$1$ 表示为车，$2$ 表示为后。

## 输出

一行一个整数，表示没有被控制到的格子数量。

## 解题思路

## AC代码
## AC代码

```cpp
#include<bits/stdc++.h>
using namespace std;

/*
思路：将车和后可以控制到的点全部标记为1
*/
//前4个方向是车的方向，8方向是后的方向
int fx[9] = {0,0,1,0,-1,-1,-1,1,1};
int fy[9] = {0,1,0,-1,0,-1,1,1,-1};

const int N = 1010;
int a[N][N];//地图
int r[N][N];//标记的结果
int n;

void fun(int x,int y){
	int ox = x,oy = y;//存储出发点 
	r[x][y] = 1;//标记出发点
	 
	//尝试8个方向 
	for(int i = 1;i <= 8;i++){
		while(a[x+fx[i]][y+fy[i]]==0){
			r[x+fx[i]][y+fy[i]]=1;//走过标记
			x = x + fx[i];
			y = y + fy[i];//移动到下一个点 
		}
		x = ox;
		y = oy;//当一个方向走到底，回撤到出发点 
		
		//如果是车，那么到第4个方向就可以结束
		if(a[ox][oy]==1 && i == 4) break;  
	} 
} 

int main() {
	cin>>n;
	memset(a,-1,sizeof(a));//将a标记为-1，这样出边界不可能是0 
	for(int i = 1; i <= n; i++) {
		for(int j = 1; j <= n; j++) {
			cin>>a[i][j];
		}
	}
	
	//从每个点开始尝试
	for(int i = 1; i <= n; i++) {
		for(int j = 1; j <= n; j++) {
			if(a[i][j] != 0) {
				fun(i,j);//从i,j点开始标记
			}
		}
	}
	
	//统计有多少个点没有被标记到
	int cnt = 0;
	for(int i = 1; i <= n; i++) {
		for(int j = 1; j <= n; j++) {
			if(r[i][j] == 0) {
				cnt++;
			}
		}
	} 
	
	cout<<cnt;
	return 0;
}
```

