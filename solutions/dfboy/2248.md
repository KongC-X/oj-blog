---
title: "画矩形"
tags: ["基础问题", "嵌套循环"]
difficulty: 入门
source: dfboy
problem_id: 2248
---

# 2248 - 画矩形

## 题目描述

根据参数，画出矩形。

## 输入

输入一行，包括四个参数：前两个参数为整数，依次代表矩形的高和宽（高不少于 $3$ 行不多于 $10$ 行，宽不少于 $5$ 列不多于 $10$ 列）；第三个参数是一个字符，表示用来画图的矩形符号；第四个参数为 $1$ 或 $0$ ，$0$ 代表空心，$1$ 代表实心。

## 输出

输出画出的图形。

## 解题思路

## AC代码
## AC代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
	int n,m;//高和宽
	char c;//字符
	int p;//标记
	cin>>n>>m>>c>>p;
	//如果是实心
	if(p == 1) {
		for(int i = 1; i <= n; i++) {
			for(int j = 1; j <= m; j++) {
				cout<<c;
			}
			cout<<endl;
		}
	} else {
		//空心
		for(int i = 1; i <= n; i++) {
			for(int j = 1; j <= m; j++) {
				//如果是第一行或者最后一行，输出m个c
				if(i == 1 || i == n) {
					cout<<c;
				} else {
					//不是第1行或最后一行，只有第1个字符和最后一个字符是c，其余的是空格
					if(j == 1 || j == m) {
						cout<<c;
					} else {
						cout<<' ';
					}
				}
			}
			cout<<endl;
		}
	}
	return 0;
}
```

