---
title: "过河问题"
tags: ["信息素养大赛", "23年复赛中学组"]
difficulty: 入门
source: dfboy
problem_id: 6054
---

# 6054 - 过河问题

## 题目描述

有 $n$ 个人要渡河，但只有一条小船，这条小船一次只能坐下最多两个人，并且只有一副船桨。每个人划船的速度不一样，如果两个人一起上船，由于重量变大，划船的速度基本上相当于是划船速度最慢的那个人速度。

假设给出每个人单独划船过河所花费的时间 $T_i$，请问所有人都过河的总时间最短的时间？

## 输入

输入两行，第一行是一个整数，表示要过河的 $n$ 个人。
第二行，是 $n$ 个整数，按速度从快到慢排序好的每个人划船过河的时间。

## 输出

输出一行，给出所有人过河所花费最短的时间。
## AC代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100005;
long long a[MAXN];               // a[i]: 第i个人的过河时间，已按升序排列（a[0]最快）

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n;                       // n: 总人数
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    // ---------- 思路 ----------
    // 经典“过河问题”，每次尽可能送走最慢的两个人。
    // 有两种送法：
    // 1) 最快者来回送：a[0]带a[i]过去，a[0]回来，a[0]带a[i-1]过去，a[0]回来。
    //    耗时 = 2*a[0] + a[i] + a[i-1]。
    // 2) 最快和次快合作送：a[0]和a[1]过去，a[0]回来，a[i-1]和a[i]过去，a[1]回来。
    //    耗时 = a[0] + 2*a[1] + a[i]。
    // 每次选择耗时较少的方案，然后问题规模减少2。
    // 边界：剩余3人时，耗时 = a[0] + a[1] + a[2]（最快来回实际上也是这个值）。
    //       剩余2人时，耗时 = a[1]（一起过）。
    //       剩余1人时，耗时 = a[0]。
    // 由于输入已排序（升序），直接使用。

    long long ans = 0;           // 总时间
    int i = n - 1;               // i: 当前最慢者的下标
    while (i >= 3) {             // 至少还有4个人（下标0,1 和 i-1,i）
        long long plan1 = 2 * a[0] + a[i] + a[i - 1];
        long long plan2 = a[0] + 2 * a[1] + a[i];
        if (plan1 < plan2)
            ans += plan1;
        else
            ans += plan2;
        i -= 2;                  // 送走最慢的两个
    }
    if (i == 2) {                // 剩下3个人：0,1,2
        ans += a[0] + a[1] + a[2];
    } else if (i == 1) {         // 剩下2个人：0,1
        ans += a[1];             // 一起过，耗时较长者
    } else if (i == 0) {         // 剩下1个人
        ans += a[0];
    }

    cout << ans << '\n';
    return 0;
}
```

