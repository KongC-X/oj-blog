---
title: "小明有没有考到90分"
tags: ["分支问题"]
difficulty: 入门
source: dfboy
problem_id: 1301
---

# 1301 - 小明有没有考到90分

## 题目描述

小明同学有一个习惯，如果考试考到 $90$ 分及以上，他就会说 `yes` ，如果没有考到，他就不说话。

请从键盘读入一个整数 $n$ 代表小明同学 的考试成绩，判断是否要输出 `yes` 。

## 输入

一个整数 $n$ 代表小明同学的考试成绩。

## 输出

如果考到 $90$ 分及以上，输出 `yes`，否则不输出。

## 解题思路

## AC代码

```cpp
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

struct Shop{
    int price; // 单价
    int num; // 数量
}a[1100];

bool cmp(Shop a,Shop b) {
    if(a.price > b.price) {
        return false;
    } else {
        return true;
    }
}

int main() {
    int i,m,n,k = 0,s = 0;
    cin >> m >> n;
    for(i = 0;i < n;i++) {
        cin >> a[i].price >> a[i].num;
    }
    // 按照单价从低到高排序
    sort(a,a+n,cmp);
    // 输出验证
    // for(i = 0;i < n;i++) {
    //     cout << a[i].price << " " << a[i].num << endl;
    // }
    for(i = 0;i < n;i++) {
        k += a[i].num;
        s += a[i].price * a[i].num;
        if(k >= m) {
            s = s - a[i].price * (k - m);
            break;
        }
    }
    cout << s << endl;
}
```
