---
title: "数字之和为13的整数"
tags: ["函数问题"]
difficulty: 入门
source: dfboy
problem_id: 1511
---

# 1511 - 数字之和为13的整数

## 题目描述

求出 $1 \sim n$ 范围内的整数，使其数字之和为 $13$，请问这样的数有多少个？ 

例如：数 $85$ ，其数字之和为 $8+5=13$ ；数 $373$，其数字之和为 $3+7+3=13$ 。

## 输入

一个整数 $n$（$n \le 10000000$）；

## 输出

输出一个整数，代表符合条件数的总个数。
## AC代码

```cpp
#include <iostream>
using namespace std;

// 求一个数的各位数字之和
int numSum(int num) {
    int sum = 0;
    while(num != 0) {
        sum += num % 10;
        num = num / 10;
    }
    return sum;
}

int main(){
    int i,n,s,a,sum = 0;
    cin >> n;
    for(i = 1;i <= n; i++) {
        s = 0;
        a = i;
        s = numSum(a);
        if(s == 13) {
            sum++;
        }
    }
    cout << sum << endl;
}
```

