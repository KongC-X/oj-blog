---
title: "二分查找右侧边界"
tags: ["二分", "分治"]
difficulty: 入门
source: dfboy
training: ["二分"]
problem_id: 1895
---

# 1895 - 二分查找右侧边界

## 题目描述

请在一个有序不递减的数组中（数组中的值有相等的值），采用二分查找，找到最后 $1$ 次出现值 $x$ 的位置，如果不存在 $x$ 请输出 $-1$。

请注意：本题要求出 $q$ 个 $x$，每个 $x$ 在数组中最后一次出现的位置。

比如有 $6$ 个数，分别是：$1$ $2$ $2$ $2$ $3$ $3$，那么如果要求 $3$ 个数：$3$ $2$ $5$，在数组中最后一次出现的位置，答案是：$6$ $4$ $-1$。

## 输入

第一行，一个整数 $n$，代表数组元素个数（$n \le 10^5$）

第二行，$n$ 个整数，用空格隔开，代表数组的 $n$ 个元素（$1 \le $数组元素的值$ \le 10^8$）

第三行，一个整数 $q$ ，代表有要求出 $q$ 个数最后一次出现的位置（$q \le 10^5$）

第四行，$q$ 个整数，用空格隔开，代表要找的数（$1 \le $要找的数$ \le 10^8$）

## 输出

按题意输出位置或者 $-1$。
## AC代码
## AC代码

```cpp
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

const int N = 100100;
int a[N];
int n,q,r = -1; //n 个数，q 次查询

int main() {
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        scanf("%d", &a[i]);
    }
    scanf("%d", &q);
    int x;
    for(int i = 1; i <= q; i++) {
        scanf("%d", &x);
        r = -1; // 每次查询前将 r 初始化为 -1
        if(binary_search(a + 1, a + n + 1, x)) {
            r = upper_bound(a + 1, a + n + 1, x) - a - 1;
        }
        printf("%d ", r);
    }
}
```

