---
title: "Pell数列"
tags: ["递推"]
difficulty: 入门
source: dfboy
training: ["电子学会考级考试"]
problem_id: 2237
---

# 2237 - Pell数列

## 题目描述

Pell数列 $a_1, a_2, a_3, \dots ,a_n$ 的定义是这样的，$a_1 = 1, a_2 = 2, ... , a_n = 2 \times a$$n − 1$$+a$$n - 2$ $(n > 2)$ 。

给出一个正整数 $k$ ，要求Pell数列的第 $k$ 项模上 $32767$ 是多少。

## 输入

第 $1$ 行是测试数据的组数 $n$ ，后面跟着 $n$ 行输入。

每组测试数据占 $1$ 行，包括一个正整数 $k$ ( )。

## 输出

$n$ 行，每行输出对应一个输入。输出应是一个非负整数。

## 解题思路

## AC代码
## AC代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int a[1000010];//存储Pell数列
int n, x;
int main()
{
	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		cin >> x; //求第x项
		a[1] = 1;
		a[2] = 2;
		//从第3项开始推导
		for (int j = 3; j <= x; j++)
		{
			a[j] = (a[j - 1] * 2 + a[j - 2]) % 32767;
		}
		cout << a[x] << endl;
	}
	return 0;
}
```

