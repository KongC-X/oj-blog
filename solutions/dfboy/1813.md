---
title: "找出最大的圆"
tags: ["数组问题"]
difficulty: 入门
source: dfboy
problem_id: 1813
---

# 1813 - 找出最大的圆

## 题目描述

给定一批圆的半径（半径值互不相同），找出这些圆中面积最大的圆，输出它的编号和面积。

圆的面积 = $ 3.14 \times $  半径 $ \times$ 半径 ，面积输出时保留 $2$ 位小数。

## 输入

第一行一个整数 $n$（$ n \lt 100 $)；

第二行 $n$ 个以空格相隔的整数，代表这 $n$ 个圆的半径（半径值都在 $1 \sim 1000$ 之间)。

## 输出

输出有 $2$ 个数，用一个空格隔开。

第 $1$ 个数代表最大圆的面积（保留 $2$ 位小数）。

第 $2$ 个数代表这个最大圆是这组数的第几个圆。

## 解题思路

## AC代码

```cpp
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

const int N = 100100;
int a[N];
int n,q; // n个数，q次查询

int fun(int x) {
    int l = 1,r = n,mid;
    while(l <= r) {
        mid = (l + r) >> 1; // 右移运算符，相当于/2
        if(x < a[mid]) r = mid - 1;
        else if(x > a[mid]) l = mid + 1;
        else if(x == a[mid]) l = mid + 1; // 相等右移
    }
    // l就是数组中第一个大于x的数的位置
    if(a[l - 1] == x) return l - 1;
    else return -1;
}

int main() {
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        scanf("%d", &a[i]);
    }
    scanf("%d", &q);
    int x;
    for(int i = 1; i <= q; i++) {
        scanf("%d", &x);
        // 求x在数组中首次出现的位置
        // cout << fun(x) << " ";
        printf("%d ", fun(x));
    }
}
```
