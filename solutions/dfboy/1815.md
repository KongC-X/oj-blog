---
title: "最后一次出现该数的位置"
tags: ["数组问题"]
difficulty: 入门
source: dfboy
training: ["一维数组"]
problem_id: 1815
---

# 1815 - 最后一次出现该数的位置

## 题目描述

从键盘输入一个整数 $N$ ，输入 $N$ ($5 \le N \le 100$)个元素，输入一个整数，判断这个数组中最后一次出现该数的位置，如果没有请输出$-1$。

## 输入

第 $1$ 行读入一个整数 $N$。（$5 \le N \le 100$）

第 $2$ 行读入 $n$ 个整数用空格隔开。

第 $3$ 行读入 $1$ 个整数，代表要找的数。

## 输出

输出 $1$ 个整数，代表最后一次出现整数的位置，如果数组中不存在这样的数，请输出 $-1$。

## 解题思路

## AC代码
## AC代码

```cpp
#include<iostream>
using namespace std;

int a[110],n,s = -1,x;

int main() {
	cin>>n;
	for(int i = 0;i < n;i++){
		cin>>a[i];
    }
    cin >> x;
    for(int i = 0;i < n;i++){
        if(a[i] == x){
            s = i + 1;
        }
    }
    cout << s << endl;
}
```

