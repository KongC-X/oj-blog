---
title: "找第一个只出现一次的字符"
tags: ["字符型"]
difficulty: 入门
source: dfboy
training: ["电子学会考级考试"]
problem_id: 2234
---

# 2234 - 找第一个只出现一次的字符

## 题目描述

给定一个只包含小写字母的字符串，请你找到第一个仅出现一次的字符。如果没有，输出 `no` 。

## 输入

一个字符串，长度小于 $100000$ 。

## 输出

输出第一个仅出现一次的字符，若没有则输出 `no` 。

## 解题思路

## AC代码
## AC代码

```cpp
#include <bits/stdc++.h>
using namespace std;

/*
思路：使用数组计数法统计每个字母出现的次数
*/
int a[200];//计数数组，统计每个字母出现的次数
string s;

int main(){
  cin>>s;
  //统计每个字母出现的次数
  for(int i = 0;i < s.size();i++){
    a[s[i]]++;
  }
  
  //统计出哪个字母是第一个出现一次的字母
  for(int i = 0;i < s.size();i++){
    if(a[s[i]]==1){
      cout<<s[i];
      return 0;
    }
  }
  
  //如果没有找到第一个出现一次的字母
  cout<<"no";
  
  return 0;
}
```

