---
title: "有趣的数字图形IV"
tags: ["二维数组"]
difficulty: 入门
source: dfboy
training: ["二维数组"]
problem_id: 1207
---

# 1207 - 有趣的数字图形IV

## 题目描述

输入一个整数 $n$ （ $n \le 12$ ），打印出如下要求的方阵：

除掉右上到左下对角线上的数外的右下半个区域中每个元素等于左边的和上面的元素之和。每个元素场宽为 $5$ 。左上半个区域为空。

## 输入

一个整数 $n$ （ $n \le 12$ ）

## 输出

$n \times n$ 的方阵（场宽为 $5$ ）。

## 解题思路

## AC代码
## AC代码

```cpp
#include<bits/stdc++.h>
using namespace std;
int n,a[20][20];
int main()
{
	cin>>n;
	for(int i=1;i<=n;i++){
		for(int j=1;j<=n;j++){
			if(i+j<n+1) cout<<setw(5)<<' ';
				else{
					if(j!=n-i+1){
						a[i][j]=a[i-1][j]+a[i][j-1];
						}
					if(j==n-i+1){
						a[i][j]=n-i+1;
						}
					cout<<setw(5)<<a[i][j];
				}
			}
				cout<<endl;
			}
		return 0;
}
```

