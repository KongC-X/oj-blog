---
title: "暑假的旅游计划"
tags: ["分支问题"]
difficulty: 入门
source: dfboy
problem_id: 1626
---

# 1626 - 暑假的旅游计划

## 题目描述

期末考试结束了，小华语文、数学、英语三门功课分别考了 $x、y、z$ 分，小华的家长说，如果小华三门功课的平均分在$90$ 分或者 $90$ 分以上，那么就去北京旅游，如果在 $90$ 分以下，那么就去南京玩。

请从键盘读入小华三门功课的成绩，编程计算出小华暑期旅游的目的地，如果去北京，则输出“ `beijing` ”，如果去南京，则输出“ `nanjing` ”。（只要输出双引号中间的拼音，不要输出双引号）

## 输入

三个整数，代表小华三门功课的考试成绩。

## 输出

按题目要求输出小华旅游的目的地。

## 解题思路

## AC代码

```cpp
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int n,a[110][110],ma;

// 深搜：从(1,1)开始，每次向下或向右走，求出所有路径和的最大值
void dfs(int x,int y,int sum) {
    ma = max(sum,ma);
    // 递归出口：当x > n时，说明已经遍历完所有点
    if(x <= n) {
        // cout << x << " " << y << " " << sum << endl;
        // 递归走：x+1,y 和 x+1,y+1
        dfs(x+1,y,sum+a[x+1][y]);
        dfs(x+1,y+1,sum+a[x+1][y+1]);
    }
}

int main() {
    cin >> n;
    int i,j;
    for(i = 1;i <= n;i++) {
        for(j = 1;j <= i;j++) {
            cin >> a[i][j];
        }
    }
    dfs(1,1,a[1][1]);
    cout << ma << endl;
}

// 当层级过深时，深搜递归会有大量数据重复计算导致运行超时
```
