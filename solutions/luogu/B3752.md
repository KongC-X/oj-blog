---
title: [信息与未来 2019] 新斐波那契数列
tags: [递归, 快速幂, 后缀数组, CDQ分治]
difficulty: 普及/提高-
---

# B3752 [信息与未来 2019] 新斐波那契数列

> **洛谷** | 普及/提高- | C++14 | 提交时间: 2026-04-17

**算法标签**：`递归`、`快速幂`、`后缀数组`、`CDQ分治`

## 题目描述

给定正整数 $a(a\ge1)$，新斐波那契数列 $f_a$ 按如下方式定义：
- $f_a(1) = 1$；
- $f_a(2) = a$；
- $f_a(n) = f_a(n − 1) + f_a(n − 2)\ (n > 2)$。

例如，给定 $a = 4$，有 $f_4(1) = 1, f_4(2) = 4, f_4(3) = 5, f_4(4) = 9, f_4(5) = 14, \cdots$ 现在已知新斐波那契数列中的一项 $x$，但并不知道 $n$ 和 $a$ 的值是多少。请你求出所有可能的 $n,a(n\ge2)$ 满足 $f_a(n) = x$。

## 输入格式

你需要在一个测试数据中处理多个新斐波那契数列问题。输入第一行 $T$ 表示问题的数量。

接下来 $T$ 行， 每行一个整数：待求解的 $x$。

## 输出格式

对于每个新斐波那契数列问题，按照 $n$ 从小到大的顺序，输出所有可能的 $n,a$ 满足 $f_a(n) = x$。每行输出一对 $n$ 和 $a$，由一个空格分隔。

## 样例

**样例输入 #1**
```
2
9
123```

**样例输出 #1**
```
2 9
3 8
4 4
2 123
3 122
4 61
6 24
10 3```

## AC 代码

```cpp
#include <iostream>
using namespace std;

int main() {
    // 预处理斐波那契数列 F[0]=0, F[1]=1
    long long F[50];
    F[0] = 0;
    F[1] = 1;
    for (int i = 2; i < 50; i++) {
        F[i] = F[i-1] + F[i-2];
    }

    int T;
    cin >> T;
    while (T--) {
        long long x;
        cin >> x;

        // 通过推导可知，新斐波那契数列的第 n 项可以表示为：
        // f_a(n) = F[n-1] * a + F[n-2]
        // 其中 a 是未知数，F[n-1] 和 F[n-2] 是斐波那契数列的系数
        // 枚举 n >= 2
        for (int n = 2; ; n++) {
            long long fn1 = F[n-1];  // 系数 a 的乘数
            long long fn2 = F[n-2];  // 常数项
            // 剪枝：如果 fn1 > x，由于 a 至少为 1，那么即使 a=1，第 n 项的值也至少是 fn1 * 1 + fn2，已经大于 x。
            // 而随着 n 增大，fn1 会更大，更不可能等于 x，所以直接停止循环（break）
            if (fn1 > x) break;      // a 至少为 1，若系数大于 x 则不可能有解
            long long rem = x - fn2;
            if (rem > 0 && rem % fn1 == 0) {
                long long a = rem / fn1;
                if (a >= 1) {
                    cout << n << " " << a << endl;
                }
            }
        }
    }
    return 0;
}
```
