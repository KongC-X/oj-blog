---
title: [GESP202506 六级] 最大因数
tags: [搜索, 匈牙利算法, 可并堆]
difficulty: 普及/提高-
---

# P13016 [GESP202506 六级] 最大因数

> **洛谷** | 普及/提高- | C++14 | 提交时间: 2026-06-17

**算法标签**：`搜索`、`匈牙利算法`、`可并堆`

## 题目背景

对应的选择、判断题：<https://ti.luogu.com.cn/problemset/1186>

## 题目描述

给定一棵有 $10^9$ 个结点的有根树，这些结点依次以 $1, 2, \dots, 10^9$ 编号，根结点的编号为 $1$。对于编号为 $k$（$2 \leq k \leq 10^9$）的结点，其父结点的编号为 $k$ 的因数中除 $k$ 以外最大的因数。

现在有 $q$ 组询问，第 $i$（$1 \leq i \leq q$）组询问给定 $x_i, y_i$，请你求出编号分别为 $x_i, y_i$ 的两个结点在这棵树上的距离。两个结点之间的距离是连接这两个结点的简单路径所包含的边数。

## 输入格式

第一行，一个正整数 $q$，表示询问组数。

接下来 $q$ 行，每行两个正整数 $x_i, y_i$，表示询问结点的编号。

## 输出格式

输出共 $q$ 行，每行一个整数，表示结点 $x_i, y_i$ 之间的距离。

## 样例

**样例输入 #1**
```
3
1 3
2 5
4 8```

**样例输出 #1**
```
1
2
1```

**样例输入 #2**
```
1
120 650```

**样例输出 #2**
```
9```

## AC 代码

```cpp
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

// 首先，看到树上简单路径，马上想到最近公共祖先，可是本题显然不可以建树，但是可以仿照 LCA 的思想来写。
// 进来的 x 与 y 像求 LCA 一样分别向上跳。因为一个不是 1 的数的最大因数一定比它小。
// 所以我们便可以将较大的那个数向上跳，这样我们在它们没有相遇的时候反复将较大的那个数向上跳，直到它们相遇为止。
// 简单证明一下，因为一个不是 1 的数的最大因数一定比它小，那么较大那个数的在这棵树里的深度一定在这两个数的 LCA 下方，
// 所以我们将大的那个数往上跳就是在不断接近 LCA，所以这样一定是正确的。
// 至于找一个数的最大因数，代码非常简单。只需要找到最小的一个除了 1 以外能被它整除的数。那么这个数就是它的最大因数。

// 求 x 的父节点：x 的最大真因数（除了 x 本身的最大因数）
// 即 x 除以它的最小质因数（若 x 为质数则返回 1）
int ys(int x) {
    for (int i = 2; i * i <= x; i++) {   // 枚举可能的因数
        if (x % i == 0) {
            return x / i;                 // i 是最小因数，x/i 就是最大真因数
        }
    }
    return 1;                             // x 是质数，父节点为 1
}

int main() {
    int q;                                // q: 询问组数
    cin >> q;
    while (q--) {
        int x, y;
        cin >> x >> y;

        // ---------- 思路 ----------
        // 树上每个结点的父节点为其最大真因数（质数的父节点为 1）。
        // 要计算两个结点 x,y 的距离，就是它们到最近公共祖先(LCA)的路径长度之和。
        // 由于树深度很小（每次至少除以 2，深度 ≤ log2(1e9) ≈ 30），可以直接模拟：
        //   不断将较深的结点向上移动到父节点，直到两结点相遇。
        // 移动时累加步数，最终步数即为距离。

        int ans = 0;                       // 距离累加器
        while (x != y) {
            if (x > y) {
                x = ys(x);                 // x 向上移动一步
                ans++;
            } else {
                y = ys(y);                 // y 向上移动一步
                ans++;
            }
        }
        cout << ans << endl;
    }
    return 0;
}
```
