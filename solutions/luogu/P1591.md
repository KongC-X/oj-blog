---
title: 阶乘数码
tags: [递归, 数学]
training: 【算法1-1】模拟与高精度
difficulty: 普及-
---

# P1591 阶乘数码

> **洛谷** | 普及- | C++14 | 提交时间: 2026-02-10

**算法标签**：`递归`、`数学`

## 题目描述

求 $n!$ 中某个数码出现的次数。

## 输入格式

第一行为 $t(t \leq 10)$，表示数据组数。接下来 $t$ 行，每行一个正整数 $n(n \leq 1000)$ 和数码 $a$。

## 输出格式

对于每组数据，输出一个整数，表示 $n!$ 中 $a$ 出现的次数。

## 样例

**样例输入 #1**
```
2
5 2
7 0```

**样例输出 #1**
```
1
2```

## AC 代码

```cpp
#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;
    
    while(t--) {
        int n, x;
        cin >> n >> x;
        
        // 分配足够大的数组（1000!有约2568位）
        int a[3000] = {0};
        a[0] = 1;
        int len = 1;  // 当前阶乘的位数
        
        // 计算阶乘
        for(int i = 2; i <= n; i++) {
            int carry = 0;
            
            // 每一位乘以i，并立即处理进位
            for(int j = 0; j < len; j++) {
                int product = a[j] * i + carry;
                a[j] = product % 10;
                carry = product / 10;
            }
            
            // 处理剩余的进位
            while(carry > 0) {
                a[len] = carry % 10;
                len++;
                carry /= 10;
            }
        }
        
        // 统计数字x出现的次数
        int count = 0;
        for(int i = 0; i < len; i++) {
            if(a[i] == x) {
                count++;
            }
        }
        cout << count << endl;
    }
    
    return 0;
}
```
