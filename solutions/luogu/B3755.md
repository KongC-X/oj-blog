---
title: [信息与未来 2019] 方格覆盖
tags: [模拟, 二分, 快速幂, CDQ分治, SG函数]
difficulty: 普及/提高-
---

# B3755 [信息与未来 2019] 方格覆盖

> **洛谷** | 普及/提高- | C++14 | 提交时间: 2026-04-21

**算法标签**：`模拟`、`二分`、`快速幂`、`CDQ分治`、`SG函数`

## 题目描述

给定一个 $n\times n$ 的矩形，其中从左上角开始，对角线上连续的 $k$ 个格子中有障碍物。你可以把若干 $1\times2$ 的小矩形放置到该大矩形中，要求是放置的两个小矩形不能占据相同的格子，且不能碰到障碍物。例如下图是 $n=4,k=2$ 的例子，我们放置了 $6$ 个 $1\times2$ 的小矩形。

![](https://cdn.luogu.com.cn/upload/image_hosting/ifmknyb8.png)

给定 $n,k$，请你输出一个方案，使得放置的 $1\times2$ 小矩形尽可能多。可以证明，$n=4,k=2$ 时，至多只能放置 $6$ 个小矩形。

## 输入格式

输入一行两个用空格分隔的正整数 $n,k$，表示矩形的大小和障碍物的数量。

## 输出格式

输出 $n$ 行，每行 $n$ 个整数（用任意数量的空格分隔）。放置的小矩形分别用 $1,2,\cdots$ 编号。不放置小矩形的格子输出 $0$。如有多种最优方案（放置最多数量的小矩形），输出任意一种即可。

## 样例

**样例输入 #1**
```
4 2```

**样例输出 #1**
```
0 0 1 2
3 0 1 2
3 4 4 0
5 5 6 6```

**样例输入 #2**
```
5 3```

**样例输出 #2**
```
0 8 8 9 10
1 0 0 9 10
1 3 0 0 7
2 3 5 5 7
2 4 4 6 6```

## AC 代码

```cpp
#include <iostream>
using namespace std;

// 模拟
// 首先我们可以用一个二维数组 a 来把不能放的地方标记为 1。
// 然后循环，如果当前可以放且没用放过，判断下与右是否能放且未放置过，能放就放，并且在答案二维数组 b 里标记答案。
// 最后输出答案数组即可。

int n, k;                     // n:棋盘大小, k:障碍物个数
int a[110][110];              // 障碍标记，1表示障碍
int b[110][110];              // 答案数组，存放骨牌编号
int t = 1;                    // 当前骨牌编号

int main() {
    cin >> n >> k;

    // 标记对角线前 k 个格子为障碍
    for (int i = 1; i <= k; i++) {
        a[i][i] = 1;
    }

    // 从上到下、从左到右贪心放置骨牌
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            // 如果当前格子是障碍或已被覆盖，跳过
            if (a[i][j] == 1 || b[i][j] != 0) continue;

            // 优先尝试横放（向右）
            if (j + 1 <= n && a[i][j+1] == 0 && b[i][j+1] == 0) {
                b[i][j] = t;
                b[i][j+1] = t;
                t++;
            }
            // 横放不行就尝试竖放（向下）
            else if (i + 1 <= n && a[i+1][j] == 0 && b[i+1][j] == 0) {
                b[i][j] = t;
                b[i+1][j] = t;
                t++;
            }
            // 如果都不行，该格子就只能空着（b[i][j] 保持 0）
        }
    }

    // 输出答案
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout << b[i][j] << " ";
        }
        cout << endl;
    }

    return 0;
}
```
